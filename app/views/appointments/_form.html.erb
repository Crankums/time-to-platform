<%= form_with model: appointment, local: true do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.label :location %>
<%= f.text_field :location %>
<%= f.collection_check_boxes(:days, days, :to_s, :to_s) %>
 
<%= f.label :starts_at%> <%# time %>
<%= f.time_field :starts_at %> <%# time %>
<%= f.label :ends_at %>
<%= f.time_field :ends_at %>
<%= f.label :type %>
<%= f.text_field :type %>
<!--- turn this into a selection? -->
<!--    add conditionals? 'if work, need clothes?' etc-->
<%= f.submit %>
<% end %>